export enum SkillDecisionType {
    EXISTING = "EXISTING",
    SUGGEST = "SUGGEST",
    CREATE = "CREATE"
  }

export enum SourceSkill {
    RESUME = "RESUME",
    LINKEDIN = "LINKEDIN",
    OTHER = "OTHER"
}